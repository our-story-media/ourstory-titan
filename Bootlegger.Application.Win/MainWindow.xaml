<Controls:MetroWindow x:Class="Bootlegger.App.Win.MainWindow"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bootlegger.App.Win"
        mc:Ignorable="d"
        BorderBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        WindowStartupLocation="CenterScreen"
        WindowTitleBrush="Transparent"
        Title="Our Story Titan" Height="400" Width="600" ResizeMode="CanMinimize" IsMaxRestoreButtonEnabled="False">
    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Button Content="Documentation" Click="Button_Click" />
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>
    

    <Grid>
        <Controls:TransitioningContentControl x:Name="_mainFrame" Transition="LeftReplace" />
        <Controls:ProgressRing x:Name="progress" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
        <Path Data="M0,274.08548 L300.05709,274.08548" Height="0.5" Stretch="Fill" Stroke="White" VerticalAlignment="Bottom" Opacity="0.5" Margin="0,0,0,52"/>
    </Grid>
</Controls:MetroWindow>
